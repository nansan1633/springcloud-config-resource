server:
    port: 8206
spring:
    profiles: test-file 
    application:
        name: application-test-simple
eureka:
    instance:
        prefer-ip-address: true
        instance-id: ${spring.cloud.client.ip-address}:${server.port}
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka
